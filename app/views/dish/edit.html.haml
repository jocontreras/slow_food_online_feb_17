= form_for [@menu.restaurant, @menu, @dish], url: restaurant_menu_dish_index_path do |f|
  = f.label :name
  = f.text_field :name
  = f.label :price
  = f.number_field :price
  = f.label :description
  = f.text_area :description
  = f.submit "Edit dish", class: 'button expanded'
